[package]
name = "xl-driver-sys"
version = "0.1.0"
edition = "2024"
publish = false
description = "Raw FFI bindings and loader primitives for Vector XL Driver"

[dependencies]
libloading = "0.8"
windows-sys = { version = "0.59", features = ["Win32_Foundation"] }
